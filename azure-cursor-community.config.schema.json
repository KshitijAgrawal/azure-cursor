{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Azure Cursor Community Plugin Config",
  "description": "Restrict the plugin to read-only, a resource group, or limit deletes to plugin-created RGs.",
  "type": "object",
  "properties": {
    "restrictedMode": {
      "type": "boolean",
      "default": false,
      "description": "When true, enforces scope restrictions. Set to false for full access."
    },
    "scope": {
      "type": "string",
      "enum": ["readOnly", "resourceGroup"],
      "description": "readOnly: no create/update/delete. resourceGroup: only allow operations within the specified resource group."
    },
    "resourceGroup": {
      "type": ["string", "null"],
      "description": "Required when scope is resourceGroup. All write operations must target this resource group only."
    },
    "restrictDeletesToCreatedByCursorAzure": {
      "type": "boolean",
      "default": false,
      "description": "When true, only allow deletes inside resource groups tagged created-by-cursor-azure=true."
    }
  },
  "required": ["restrictedMode"],
  "additionalProperties": false
}
